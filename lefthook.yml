pre-commit:
  commands:
    eslint:
      glob: "*.{ts,js}"
      run: npx eslint {staged_files} && git add .
    prettier:
      glob: "*.{ts,js}"
      run: npx prettier --write {staged_files} && git add .
